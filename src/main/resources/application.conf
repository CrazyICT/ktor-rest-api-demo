 ktor {
     development = true
     environment = ${?KTOR_ENV}

     deployment {
         port = 8080
         port = ${?PORT}

         watch = [ dev.mrcrazy ]
     }

     application {
         modules = [ dev.mrcrazy.ApplicationKt.module, ]
     }
 }